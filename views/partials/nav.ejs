  <% if (!user) { %>
    <link rel="stylesheet" href="/stylesheets/siwg.css">
  <% } %>
</head>
<body>
  <nav>
    <% if (user) { %>
      <a href="/auth/logout">
        <button type="submit" class="btn btn-danger">Logout</button>
      </a>
      <a href="/places">
        <button type="submit" class="btn btn-primary">Add Places</button>
      </a>
      <a href="/collections">
        <button type="submit" class="btn btn-primary">My Places</button>
      </a>
      <a href="/profiles">
        <button type="submit" class="btn btn-primary">Profiles</button>
      </a>
      <a href="/posts">
        <button type="submit" class="btn btn-primary">Post Board</button>
      </a>
    <% } else { %>
      <a href="/auth/google" class="siwg-link">
        <button type="submit" class="siwg">
          <img id="siwg-image" alt="Sign in with Google" />
        </button>
      </a>
    <% } %>
  </nav>
